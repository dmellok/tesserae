{
  "name": "tesserae-ota-signer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that signs Tesserae OTA firmware descriptors and serves images from R2.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "mint": "node scripts/mint-key.mjs"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
