{
  "tesserae_compat": "1.x",
  "name": "Clock, Sunrise / Sunset",
  "version": "0.1.6",
  "kind": "widget",
  "description": "Today's sunrise, sunset, golden hour, and day length for a chosen lat/lon. The hero shows a sun-arc visualisation with the current position marked. Data from Open-Meteo (no key).",
  "icon": "ph-sun-horizon",
  "supports": {
    "sizes": [
      "sm",
      "md",
      "lg"
    ]
  },
  "cell_options": [
    {
      "name": "location",
      "type": "location_search",
      "label": "Location",
      "default": "",
      "help": "Search a city to set the location. The latitude / longitude that drive the weather data come from the picked result; the Label below auto-fills with the city name."
    },
    {
      "name": "label",
      "type": "string",
      "label": "Label",
      "default": "",
      "help": "Auto-filled from the location pick; edit to customise (e.g. 'Home' instead of 'Berlin')."
    },
    {
      "name": "show_arc",
      "type": "boolean",
      "label": "Show sun-path arc",
      "default": true
    },
    {
      "name": "show_golden",
      "type": "boolean",
      "label": "Tint golden-hour bands",
      "default": true
    },
    {
      "name": "show_twilight",
      "type": "boolean",
      "label": "Tint twilight bands beyond the horizon (civil / nautical / astronomical)",
      "default": true
    }
  ],
  "render": {
    "dither": "none",
    "needs_network": true
  },
  "requires": [
    "network:api.open-meteo.com"
  ]
}
