{
  "tesserae_compat": "1.x",
  "name": "Weather, Now",
  "version": "0.1.10",
  "kind": "widget",
  "description": "Current conditions plus feels-like, humidity, wind, UV and sun times. Data from Open-Meteo (no API key required).",
  "icon": "ph-cloud-sun",
  "supports": {
    "sizes": [
      "xs",
      "sm",
      "md",
      "lg"
    ]
  },
  "cell_options": [
    {
      "name": "location",
      "type": "location_search",
      "label": "Location",
      "default": "",
      "help": "Search a city to set the location. The latitude / longitude that drive the weather data come from the picked result; the Label below auto-fills with the city name."
    },
    {
      "name": "label",
      "type": "string",
      "label": "Label",
      "default": "",
      "help": "Auto-filled from the location pick; edit to customise (e.g. 'Home' instead of 'Berlin')."
    },
    {
      "name": "units",
      "type": "select",
      "label": "Units",
      "default": "metric",
      "choices": [
        {
          "value": "metric",
          "label": "Metric (\u00b0C, km/h)"
        },
        {
          "value": "imperial",
          "label": "Imperial (\u00b0F, mph)"
        }
      ]
    }
  ],
  "render": {
    "dither": "none",
    "needs_network": true
  },
  "requires": [
    "network:api.open-meteo.com"
  ],
  "fragments": [
    { "id": "full", "label": "Full card", "w": 320, "h": 200, "icon": "ph-cloud-sun" },
    { "id": "temp", "label": "Temperature", "w": 180, "h": 120, "icon": "ph-thermometer-simple" },
    { "id": "metrics", "label": "Metrics strip", "w": 300, "h": 90, "icon": "ph-gauge" },
    { "id": "sun", "label": "Sun times", "w": 160, "h": 150, "icon": "ph-sun-horizon" }
  ]
}
