{
  "tesserae_compat": "1.x",
  "name": "GitHub — Repo Card",
  "version": "0.2.0",
  "kind": "widget",
  "description": "Single-repo card Each pairs the language split + stars / forks / issues / release stats with a 52-week commit activity chart. Public repos work without a token.",
  "icon": "ph-book-bookmark",
  "supports": {
    "sizes": [
      "xs",
      "sm",
      "md",
      "lg"
    ]
  },
  "cell_options": [
    {
      "name": "repo",
      "type": "string",
      "label": "owner/repo",
      "default": "torvalds/linux"
    }
  ],
  "render": {
    "dither": "none",
    "needs_network": true
  }
}
