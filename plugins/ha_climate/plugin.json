{
  "tesserae_compat": "1.x",
  "name": "Home Assistant — Climate",
  "version": "0.2.0",
  "kind": "widget",
  "description": "One or more Home Assistant climate / thermostat entities as Bauhaus tiles: current temperature, target (or range), HVAC mode and what each is doing right now (heating / cooling / idle), tinted accordingly. Requires the Home Assistant Core plugin.",
  "icon": "ph-thermometer-simple",
  "supports": { "sizes": ["sm", "md", "lg"] },
  "cell_options": [
    { "name": "title", "type": "string", "label": "Title (blank = entity name / \"Climate\")", "default": "" },
    { "name": "entities", "type": "multiselect", "label": "Climate entities", "default": [], "choices_from": "entity" }
  ],
  "render": { "dither": "none", "needs_network": true }
}
