{
  "tesserae_compat": "1.x",
  "name": "GitHub — Actions",
  "version": "0.1.0",
  "kind": "widget",
  "description": "Most recent CI runs across watched repos with status pills (success / failure / in-progress). Public repos work without a token; private repos require the PAT in Plugins → GitHub Core.",
  "icon": "ph-play-circle",
  "supports": { "sizes": ["sm", "md", "lg"] },
  "cell_options": [
    { "name": "repos",       "type": "textarea", "label": "Repos (one per line, owner/repo)", "default": "" },
    { "name": "max_results", "type": "number",   "label": "Max runs per repo",                 "default": 3 }
  ],
  "render": { "dither": "none", "needs_network": true }
}
