{
  "tesserae_compat": "1.x",
  "name": "Pi BIN client (4-bpp .bin)",
  "version": "0.1.0",
  "description": "Composition PNG packed into the panel-native 4-bpp buffer the .bin Pi client consumes. Published non-retained to tesserae/pi_bin/frame/bin. Shares its packed bytes (and on-disk file) with esp32_bin when both targets are active.",
  "orientation": "composition",
  "mime": "application/octet-stream",
  "extension": "bin",
  "topic_pattern": "tesserae/{device}/frame/bin",
  "device": "pi_bin",
  "retain": false,
  "settings": [
    {
      "name": "dither",
      "type": "select",
      "label": "Dither mode",
      "default": "floyd-steinberg",
      "choices": [
        { "value": "floyd-steinberg", "label": "Floyd-Steinberg (smooth gradients)" },
        { "value": "atkinson", "label": "Atkinson (contrasty, clean solids)" },
        { "value": "jarvis", "label": "Jarvis-Judice-Ninke (smoothest)" },
        { "value": "stucki", "label": "Stucki" },
        { "value": "bayer-8x8", "label": "Bayer 8x8 (ordered)" },
        { "value": "halftone", "label": "Halftone dots" },
        { "value": "crosshatch", "label": "Crosshatch" },
        { "value": "none", "label": "None (snap to palette)" }
      ]
    },
    {
      "name": "saturation",
      "type": "slider",
      "label": "Pre-quantise saturation",
      "default": 1.4,
      "min": 0,
      "max": 3,
      "step": 0.05,
      "help": "Spectra 6 has only six pure colours; boosting saturation before the dither pass pushes near-palette tones onto exact palette values, killing speckle. 1.4 is the recommended out-of-the-box value; bump to 1.6+ for photos."
    },
    {
      "name": "contrast",
      "type": "slider",
      "label": "Pre-quantise contrast",
      "default": 1.0,
      "min": 0,
      "max": 3,
      "step": 0.05
    }
  ]
}
