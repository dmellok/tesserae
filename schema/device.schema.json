{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://tesserae.local/schema/device.schema.json",
  "title": "Tesserae device manifest",
  "description": "Shape of device.json. A device is the consumer side of one or more renderers: it subscribes to a status heartbeat topic, optionally publishes to a config topic, and (informationally) lists which renderers it can ingest.",
  "type": "object",
  "required": ["tesserae_compat", "name", "version", "renderers", "status_topic"],
  "properties": {
    "tesserae_compat": {
      "type": "string",
      "pattern": "^\\d+\\.(x|\\d+)(\\..*)?$"
    },
    "name": { "type": "string", "minLength": 1 },
    "icon": {
      "type": "string",
      "description": "Phosphor icon slug (no 'ph-' prefix) shown in device pickers and the settings card. Instances may override the kind's default."
    },
    "version": { "type": "string", "pattern": "^\\d+\\.\\d+\\.\\d+(?:[-+].*)?$" },
    "description": { "type": "string" },
    "renderers": {
      "type": "array",
      "items": { "type": "string", "pattern": "^[a-z][a-z0-9_]*$" },
      "minItems": 1,
      "uniqueItems": true,
      "description": "Renderer ids this device consumes. Informational for the UI; renderers run regardless of which devices are loaded."
    },
    "status_topic": {
      "type": "string",
      "minLength": 1,
      "description": "MQTT topic the device publishes its heartbeat to. Tesserae subscribes and parses payloads through device.parse_status()."
    },
    "config_topic": {
      "type": "string",
      "minLength": 1,
      "description": "MQTT topic for outbound config. Omit if the device has no configurable knobs."
    },
    "config_schema": {
      "type": "object",
      "description": "Field definitions for the config form. Each value is a {type, default, min?, max?, label?, choices?} dict.",
      "additionalProperties": {
        "type": "object",
        "required": ["type"],
        "properties": {
          "type": { "enum": ["int", "float", "string", "boolean", "select"] },
          "label": { "type": "string" },
          "default": {},
          "min": { "type": "number" },
          "max": { "type": "number" },
          "choices": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["value", "label"],
              "properties": { "value": {}, "label": { "type": "string" } }
            }
          },
          "presets": {
            "type": "array",
            "description": "For numeric fields: a set of named preset values shown in a dropdown alongside a 'Custom…' option that reveals a number input. Submitted value is whichever the user landed on.",
            "items": {
              "type": "object",
              "required": ["value", "label"],
              "properties": { "value": { "type": "number" }, "label": { "type": "string" } }
            }
          },
          "help": { "type": "string" }
        }
      }
    },
    "panel": {
      "type": "object",
      "description": "Physical panel attached to this device. Multi-head installs use this to size each device's compositions independently; pages can be assigned to a specific device and inherit its panel.",
      "required": ["w", "h"],
      "properties": {
        "w": { "type": "integer", "minimum": 1 },
        "h": { "type": "integer", "minimum": 1 },
        "orientation": {
          "type": "string",
          "enum": ["landscape", "landscape_flipped", "portrait", "portrait_flipped"],
          "description": "How the user wants to read the dashboard. Landscape/portrait sets the canvas aspect; the '_flipped' variants add a 180° turn the renderer applies for an upside-down physical mount. The renderer already maps the composition onto its client's fixed buffer direction."
        },
        "name": { "type": "string", "description": "Friendly name shown in the UI." }
      }
    }
  }
}
