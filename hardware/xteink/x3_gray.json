{
  "tesserae_compat": "1.x",
  "id": "xteink_x3_gray",
  "name": "Xteink X3 (4-level grayscale)",
  "vendor": "Xteink",
  "url": "https://www.xteink.com/",
  "icon": "device-tablet",
  "description": "Xteink X3 driven in its 4-level grayscale mode rather than pure 1-bit mono. Same 792x528 panel mounted portrait (528x792 presentation) and the same CrossInk sleep-screen client as xteink_x3, but the server packs a 2-bpp buffer (104544 bytes) via esp32_gray2_bin and the firmware composites it as a base frame plus two grayscale planes. Pick this over xteink_x3 when the dashboard has photos or shading; stay on xteink_x3 for text and line art, where the extra levels buy little and the frame is half the size.",
  "protocol": "esp32_bw_client",
  "panel": {
    "w": 528,
    "h": 792,
    "orientation": "portrait_flipped",
    "name": "ePaper, 4-level grayscale (792x528 native)",
    "gamut": "gray_4",
    "native_w": 792,
    "native_h": 528
  },
  "renderers": [
    "esp32_gray2_bin"
  ],
  "refresh_floor_s": 60,
  "image_format": "bin",
  "notes_md": "Confirmed on real hardware running CrossInk with Tesserae client support: a 4-level grayscale dashboard renders upright in portrait on an Xteink X3.\n\nWire format is exactly 104544 bytes (792*528/4), 4 px/byte, MSB-first, `0b00` black to `0b11` white, linear. That packing matches CrossInk's own 2-bpp bitmap reader, so the firmware expands the frame straight into its `GRAYSCALE_LSB` / `GRAYSCALE_MSB` planes with no decode. The X3 additionally needs the OEM preconditioning settle pass between the base frame and the planes, which the firmware issues unconditionally (it is a no-op on the X4).\n\nWorth noting for anyone extending `esp32_gray2_bin`: this renderer is described as targeting UC8179-class silicon, and the X3 is UC8253 / UC8279d. It works there too. Only the UC8279d production run has been exercised so far.\n\n`portrait_flipped` is confirmed rather than inherited: the X3 has a different controller family from the X4 but the same framebuffer scan origin, so the same 180-degree correction applies."
}
